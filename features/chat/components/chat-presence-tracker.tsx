/**
 * @deprecated Presence is handled inside `useChatPresence` (callbacks before subscribe).
 * Kept so old imports do not break.
 */
export function ChatPresenceTracker(_props: { userId: string }) {
  return null;
}
